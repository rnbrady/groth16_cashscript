pragma cashscript ^0.13.0;
// vk_x chunk 15: term 1, iterations [224,254), fold=True, reset_to_ic2=False, final=True.
contract VkxChunk15() {
    internal function addFp(int x, int y) returns (int) { return (x + y) % 21888242871839275222246405745257275088696311157297823662689037894645226208583; }
    internal function subFp(int x, int y) returns (int) { return (x - y + 21888242871839275222246405745257275088696311157297823662689037894645226208583) % 21888242871839275222246405745257275088696311157297823662689037894645226208583; }
    internal function mulFp(int x, int y) returns (int) { return (x * y) % 21888242871839275222246405745257275088696311157297823662689037894645226208583; }
    internal function sqrFp(int x) returns (int) { return (x * x) % 21888242871839275222246405745257275088696311157297823662689037894645226208583; }
    internal function inverseFp(int x) returns (int) {
        int e = 21888242871839275222246405745257275088696311157297823662689037894645226208583 - 2;
        int result = 1;
        int current = x % 21888242871839275222246405745257275088696311157297823662689037894645226208583;
        for (int i = 0; i < 254; i++) {
            if (((e >> i) % 2) == 1) { result = (result * current) % 21888242871839275222246405745257275088696311157297823662689037894645226208583; }
            current = (current * current) % 21888242871839275222246405745257275088696311157297823662689037894645226208583;
        }
        return result;
    }
    function spend(int accX, int accY, int accZ, int bX, int bY, int bZ, int rX, int rY, int rZ) {
        require(hash256(toPaddedBytes(accX, 40) + toPaddedBytes(accY, 40) + toPaddedBytes(accZ, 40) + toPaddedBytes(bX, 40) + toPaddedBytes(bY, 40) + toPaddedBytes(bZ, 40) + toPaddedBytes(rX, 40) + toPaddedBytes(rY, 40) + toPaddedBytes(rZ, 40)) == 0x2cdc7e8c9eda1e6cb2a0a4ee516c21f9b12c9afa51fc0f0cea030d2db8453329);
        int input = 987654321;
        for (int i = 224; i < 254; i = i + 1) {
            if (((input >> i) % 2) == 1) {
                if (rZ == 0) {
                    rX = bX; rY = bY; rZ = bZ;
                } else {
            int z1z1 = sqrFp(rZ);
            int z2z2 = sqrFp(bZ);
            int u1 = mulFp(rX, z2z2);
            int u2 = mulFp(bX, z1z1);
            int s1 = mulFp(mulFp(rY, bZ), z2z2);
            int s2 = mulFp(mulFp(bY, rZ), z1z1);
            if (u1 == u2 && s1 == s2) {
                int a = sqrFp(rX);
                int b = sqrFp(rY);
                int c = sqrFp(b);
                int d = mulFp(2, subFp(subFp(sqrFp(addFp(rX, b)), a), c));
                int e = mulFp(3, a);
                int f = sqrFp(e);
                int nx = subFp(f, mulFp(2, d));
                int ny = subFp(mulFp(e, subFp(d, nx)), mulFp(8, c));
                int nz = mulFp(2, mulFp(rY, rZ));
                rX = nx; rY = ny; rZ = nz;
            } else {
                int h = subFp(u2, u1);
                int i2 = sqrFp(mulFp(2, h));
                int j = mulFp(h, i2);
                int rr = mulFp(2, subFp(s2, s1));
                int vv = mulFp(u1, i2);
                int nx = subFp(subFp(sqrFp(rr), j), mulFp(2, vv));
                int ny = subFp(mulFp(rr, subFp(vv, nx)), mulFp(2, mulFp(s1, j)));
                int nz = mulFp(subFp(subFp(sqrFp(addFp(rZ, bZ)), z1z1), z2z2), h);
                rX = nx; rY = ny; rZ = nz;
            }
                }
            }
            if (bZ != 0 && bY != 0) {
            int a = sqrFp(bX);
            int b = sqrFp(bY);
            int c = sqrFp(b);
            int d = mulFp(2, subFp(subFp(sqrFp(addFp(bX, b)), a), c));
            int e = mulFp(3, a);
            int f = sqrFp(e);
            int nx = subFp(f, mulFp(2, d));
            int ny = subFp(mulFp(e, subFp(d, nx)), mulFp(8, c));
            int nz = mulFp(2, mulFp(bY, bZ));
            bX = nx; bY = ny; bZ = nz;
            }
        }
        if (rZ != 0) {
            int z1z1 = sqrFp(accZ);
            int z2z2 = sqrFp(rZ);
            int u1 = mulFp(accX, z2z2);
            int u2 = mulFp(rX, z1z1);
            int s1 = mulFp(mulFp(accY, rZ), z2z2);
            int s2 = mulFp(mulFp(rY, accZ), z1z1);
            if (u1 == u2 && s1 == s2) {
                int a = sqrFp(accX);
                int b = sqrFp(accY);
                int c = sqrFp(b);
                int d = mulFp(2, subFp(subFp(sqrFp(addFp(accX, b)), a), c));
                int e = mulFp(3, a);
                int f = sqrFp(e);
                int nx = subFp(f, mulFp(2, d));
                int ny = subFp(mulFp(e, subFp(d, nx)), mulFp(8, c));
                int nz = mulFp(2, mulFp(accY, accZ));
                accX = nx; accY = ny; accZ = nz;
            } else {
                int h = subFp(u2, u1);
                int i2 = sqrFp(mulFp(2, h));
                int j = mulFp(h, i2);
                int rr = mulFp(2, subFp(s2, s1));
                int vv = mulFp(u1, i2);
                int nx = subFp(subFp(sqrFp(rr), j), mulFp(2, vv));
                int ny = subFp(mulFp(rr, subFp(vv, nx)), mulFp(2, mulFp(s1, j)));
                int nz = mulFp(subFp(subFp(sqrFp(addFp(accZ, rZ)), z1z1), z2z2), h);
                accX = nx; accY = ny; accZ = nz;
            }
        }
        int zInv = inverseFp(accZ);
        int zInv2 = sqrFp(zInv);
        int zInv3 = mulFp(zInv2, zInv);
        require(mulFp(accX, zInv2) == 9749522656125667161218610527789566824082547561737311503154242585977001677528);
        require(mulFp(accY, zInv3) == 11261491184979604396731387498248109768593573743752840655503305098314920652045);
    }
}
